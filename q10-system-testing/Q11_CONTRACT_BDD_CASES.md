# Q11 Generated Express–Next Contract BDD Cases

Generated from 176 frontend HTTP call sites.

## Q11-CONTRACT-001: POST /api/v1/master-data/parties

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\crm\CrmClient.tsx` invokes `POST /api/v1/master-data/parties`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-002: POST /api/v1/finance/journal-entries/:param/reverse

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `POST /api/v1/finance/journal-entries/:param/reverse`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-003: PATCH /api/v1/finance/project-cost-entries/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `PATCH /api/v1/finance/project-cost-entries/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-004: PATCH /api/v1/finance/project-fundings/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `PATCH /api/v1/finance/project-fundings/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-005: PATCH /api/v1/finance/billing-proposals/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `PATCH /api/v1/finance/billing-proposals/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-006: PATCH /api/v1/finance/billing-documents/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `PATCH /api/v1/finance/billing-documents/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-007: PATCH /api/v1/finance/billing-documents/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `PATCH /api/v1/finance/billing-documents/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-008: POST /api/v1/finance/project-cost-entries

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `POST /api/v1/finance/project-cost-entries`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-009: POST /api/v1/finance/project-fundings

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `POST /api/v1/finance/project-fundings`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-010: POST /api/v1/finance/billing-proposals

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `POST /api/v1/finance/billing-proposals`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-011: POST /api/v1/finance/billing-documents

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `POST /api/v1/finance/billing-documents`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-012: POST /api/v1/finance/customer-receipts

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `POST /api/v1/finance/customer-receipts`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-013: POST /api/v1/finance/project-fundings/:param/decide

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `POST /api/v1/finance/project-fundings/:param/decide`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-014: DELETE /api/v1/finance/project-fundings/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\finance\FinanceClient.tsx` invokes `DELETE /api/v1/finance/project-fundings/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-015: GET /api/v1/projects/control-items

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\projects\ProjectsClient.tsx` invokes `GET /api/v1/projects/control-items`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-016: POST /api/v1/projects/control-items

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\projects\ProjectsClient.tsx` invokes `POST /api/v1/projects/control-items`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-017: GET /api/v1/projects/control-items

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\projects\ProjectsClient.tsx` invokes `GET /api/v1/projects/control-items`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-018: PATCH /api/v1/projects/control-items/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\projects\ProjectsClient.tsx` invokes `PATCH /api/v1/projects/control-items/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-019: GET /api/v1/core/organizations

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\projects\ProjectsClient.tsx` invokes `GET /api/v1/core/organizations`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-020: POST /api/v1/projects/projects/:param/update_financials

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\projects\ProjectsClient.tsx` invokes `POST /api/v1/projects/projects/:param/update_financials`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-021: POST /api/v1/finance/project-fundings

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\projects\ProjectsClient.tsx` invokes `POST /api/v1/finance/project-fundings`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-022: POST /api/v1/projects/projects/:param/advance-stage

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\projects\ProjectsClient.tsx` invokes `POST /api/v1/projects/projects/:param/advance-stage`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-023: PATCH /api/v1/projects/projects/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\projects\ProjectsClient.tsx` invokes `PATCH /api/v1/projects/projects/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-024: GET /api/v1/projects/projects

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\reporting\ReportingClient.tsx` invokes `GET /api/v1/projects/projects`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-025: GET /api/v1/finance/project-cost-entries

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\reporting\ReportingClient.tsx` invokes `GET /api/v1/finance/project-cost-entries`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-026: GET /api/v1/finance/billing-proposals

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\reporting\ReportingClient.tsx` invokes `GET /api/v1/finance/billing-proposals`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-027: GET /api/v1/sales/orders

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\reporting\ReportingClient.tsx` invokes `GET /api/v1/sales/orders`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-028: GET /api/v1/finance/journal-entries

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\reporting\ReportingClient.tsx` invokes `GET /api/v1/finance/journal-entries`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-029: GET /api/v1/reporting/periodic-project-summary

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\reporting\ReportingClient.tsx` invokes `GET /api/v1/reporting/periodic-project-summary`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-030: GET /api/v1/reporting/attendance-summary

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\app\(app)\reporting\ReportingClient.tsx` invokes `GET /api/v1/reporting/attendance-summary`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-031: GET /api/v1/core/companies

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\administration\AccessAdministration.tsx` invokes `GET /api/v1/core/companies`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-032: GET /api/v1/accounts/roles

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\administration\AccessAdministration.tsx` invokes `GET /api/v1/accounts/roles`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-033: GET /api/v1/core/companies/:param/modules

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\administration\AccessAdministration.tsx` invokes `GET /api/v1/core/companies/:param/modules`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-034: GET /api/v1/accounts/users

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\administration\AccessAdministration.tsx` invokes `GET /api/v1/accounts/users`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-035: GET /api/v1/core/company-modules/my-modules

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\administration\AccessAdministration.tsx` invokes `GET /api/v1/core/company-modules/my-modules`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-036: GET /api/v1/accounts/user-module-access

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\administration\AccessAdministration.tsx` invokes `GET /api/v1/accounts/user-module-access`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-037: PATCH /api/v1/core/companies/:param/modules/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\administration\AccessAdministration.tsx` invokes `PATCH /api/v1/core/companies/:param/modules/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-038: DELETE /api/v1/accounts/users/:param/module-access/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\administration\AccessAdministration.tsx` invokes `DELETE /api/v1/accounts/users/:param/module-access/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-039: PUT /api/v1/accounts/users/:param/module-access/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\administration\AccessAdministration.tsx` invokes `PUT /api/v1/accounts/users/:param/module-access/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-040: POST /api/v1/accounts/users/invite

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\administration\AccessAdministration.tsx` invokes `POST /api/v1/accounts/users/invite`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-041: GET /api/v1/finance/audit-trail

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\AuditTrailWorkspace.tsx` invokes `GET /api/v1/finance/audit-trail`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-042: GET /api/v1/core/companies

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\CompanyMasterWorkspace.tsx` invokes `GET /api/v1/core/companies`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-043: GET /api/v1/finance/bank-accounts

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\CompanyMasterWorkspace.tsx` invokes `GET /api/v1/finance/bank-accounts`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-044: PATCH /api/v1/core/companies/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\CompanyMasterWorkspace.tsx` invokes `PATCH /api/v1/core/companies/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-045: POST /api/v1/finance/bank-accounts

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\CompanyMasterWorkspace.tsx` invokes `POST /api/v1/finance/bank-accounts`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-046: POST /api/v1/finance/credit-facilities

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\CompanyMasterWorkspace.tsx` invokes `POST /api/v1/finance/credit-facilities`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-047: POST /api/v1/core/organizations

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\CompanyMasterWorkspace.tsx` invokes `POST /api/v1/core/organizations`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-048: GET /api/v1/finance/executive-audit-report

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\ExecutiveAuditReportWorkspace.tsx` invokes `GET /api/v1/finance/executive-audit-report`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-049: GET /api/v1/assets/assets

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\FixedAssetsWorkspace.tsx` invokes `GET /api/v1/assets/assets`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-050: GET /api/v1/assets/books

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\FixedAssetsWorkspace.tsx` invokes `GET /api/v1/assets/books`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-051: GET /api/v1/assets/assets/:param/depreciation-schedule

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\FixedAssetsWorkspace.tsx` invokes `GET /api/v1/assets/assets/:param/depreciation-schedule`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-052: POST /api/v1/assets/assets/:param/depreciate

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\FixedAssetsWorkspace.tsx` invokes `POST /api/v1/assets/assets/:param/depreciate`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-053: POST /api/v1/assets/assets/batch-depreciate

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\FixedAssetsWorkspace.tsx` invokes `POST /api/v1/assets/assets/batch-depreciate`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-054: POST /api/v1/assets/assets/:param/dispose

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\FixedAssetsWorkspace.tsx` invokes `POST /api/v1/assets/assets/:param/dispose`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-055: GET /api/v1/finance/fiscal-periods/status

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\PeriodClosingWorkspace.tsx` invokes `GET /api/v1/finance/fiscal-periods/status`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-056: GET /api/v1/finance/fiscal-years

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\PeriodClosingWorkspace.tsx` invokes `GET /api/v1/finance/fiscal-years`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-057: POST /api/v1/finance/period-closings/request

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\PeriodClosingWorkspace.tsx` invokes `POST /api/v1/finance/period-closings/request`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-058: POST /api/v1/finance/fiscal-periods/:param/reopen

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\PeriodClosingWorkspace.tsx` invokes `POST /api/v1/finance/fiscal-periods/:param/reopen`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-059: POST /api/v1/finance/period-closings/request

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\PeriodClosingWorkspace.tsx` invokes `POST /api/v1/finance/period-closings/request`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-060: POST /api/v1/finance/fiscal-years/:param/reopen-year-end

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\finance\PeriodClosingWorkspace.tsx` invokes `POST /api/v1/finance/fiscal-years/:param/reopen-year-end`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-061: GET /api/v1/requests/team-members

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\requests\NewCardRequestModal.tsx` invokes `GET /api/v1/requests/team-members`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-062: POST /api/v1/requests

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\requests\NewCardRequestModal.tsx` invokes `POST /api/v1/requests`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-063: GET /api/v1/requests

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\requests\RequestCardFeed.tsx` invokes `GET /api/v1/requests`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-064: POST /api/v1/requests/:param/validate-om

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\requests\RequestReviewModal.tsx` invokes `POST /api/v1/requests/:param/validate-om`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-065: POST /api/v1/requests/:param/approve-exec

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\requests\RequestReviewModal.tsx` invokes `POST /api/v1/requests/:param/approve-exec`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-066: POST /api/v1/requests/:param/disburse

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\requests\RequestReviewModal.tsx` invokes `POST /api/v1/requests/:param/disburse`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-067: POST /api/v1/requests/:param/submit-lpj

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\requests\RequestReviewModal.tsx` invokes `POST /api/v1/requests/:param/submit-lpj`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-068: POST /api/v1/requests/:param/verify-lpj-om

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\components\requests\RequestReviewModal.tsx` invokes `POST /api/v1/requests/:param/verify-lpj-om`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-069: PATCH /api/v1/auth/active-role

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\auth.api.ts` invokes `PATCH /api/v1/auth/active-role`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-070: POST /api/v1/auth/logout

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\auth.api.ts` invokes `POST /api/v1/auth/logout`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-071: GET /api/v1/core/companies

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\auth.api.ts` invokes `GET /api/v1/core/companies`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-072: POST /api/v1/auth/change-password

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\auth.api.ts` invokes `POST /api/v1/auth/change-password`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-073: POST /api/v1/auth/update-profile

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\auth.api.ts` invokes `POST /api/v1/auth/update-profile`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-074: GET /api/v1/commands/reporting/crm-sales-dashboard

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `GET /api/v1/commands/reporting/crm-sales-dashboard`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-075: POST /api/v1/crm/opportunities/:param/process-deal-won

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/opportunities/:param/process-deal-won`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-076: POST /api/v1/crm/opportunities/:param/executive-override

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/opportunities/:param/executive-override`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-077: POST /api/v1/crm/opportunities

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/opportunities`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-078: DELETE /api/v1/crm/opportunities/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `DELETE /api/v1/crm/opportunities/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-079: POST /api/v1/crm/customer-inquiries/:param/qualify

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/customer-inquiries/:param/qualify`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-080: POST /api/v1/crm/customer-inquiries

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/customer-inquiries`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-081: POST /api/v1/crm/inquiry-requirements

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/inquiry-requirements`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-082: DELETE /api/v1/crm/customer-inquiries/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `DELETE /api/v1/crm/customer-inquiries/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-083: POST /api/v1/crm/cost-estimates

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/cost-estimates`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-084: POST /api/v1/crm/cost-estimate-lines

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/cost-estimate-lines`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-085: POST /api/v1/crm/cost-estimate-lines

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/cost-estimate-lines`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-086: POST /api/v1/crm/cost-estimates/:param/calculate

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/cost-estimates/:param/calculate`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-087: POST /api/v1/crm/cost-estimates/:param/create-quotation

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/cost-estimates/:param/create-quotation`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-088: DELETE /api/v1/crm/cost-estimates/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `DELETE /api/v1/crm/cost-estimates/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-089: POST /api/v1/sales/quotations/:param/submit-approval

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/sales/quotations/:param/submit-approval`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-090: GET /api/v1/crm/executive-approvals

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `GET /api/v1/crm/executive-approvals`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-091: POST /api/v1/crm/executive-approvals/:param/decide

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/executive-approvals/:param/decide`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-092: POST /api/v1/sales/quotations/:param/send

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/sales/quotations/:param/send`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-093: POST /api/v1/sales/quotations/:param/customer-decision

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/sales/quotations/:param/customer-decision`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-094: POST /api/v1/sales/quotations/:param/customer-decision

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/sales/quotations/:param/customer-decision`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-095: POST /api/v1/commands/sales/quotations/:param/convert-to-order

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/commands/sales/quotations/:param/convert-to-order`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-096: POST /api/v1/service/cases

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/service/cases`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-097: GET /api/v1/service/cases/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `GET /api/v1/service/cases/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-098: DELETE /api/v1/service/cases/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `DELETE /api/v1/service/cases/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-099: POST /api/v1/master-data/customer-profiles/set-credit-limit

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/master-data/customer-profiles/set-credit-limit`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-100: POST /api/v1/crm/credit-status-snapshots/calculate

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `POST /api/v1/crm/credit-status-snapshots/calculate`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-101: DELETE /api/v1/crm/inquiry-requirements/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\crm.api.ts` invokes `DELETE /api/v1/crm/inquiry-requirements/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-102: POST /api/v1/sidebar-feed/mark-read

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `POST /api/v1/sidebar-feed/mark-read`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-103: POST /api/v1/core/sidebar-feed/mark-read

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `POST /api/v1/core/sidebar-feed/mark-read`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-104: POST /api/v1/recent-items/track

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `POST /api/v1/recent-items/track`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-105: POST /api/v1/core/recent-items/track

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `POST /api/v1/core/recent-items/track`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-106: GET /api/v1/recent-items

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/recent-items`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-107: GET /api/v1/core/recent-items

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/core/recent-items`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-108: GET /api/v1/projects/tasks

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/projects/tasks`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-109: GET /api/v1/projects/projects

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/projects/projects`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-110: GET /api/v1/finance/project-cost-entries

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/finance/project-cost-entries`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-111: GET /api/v1/finance/billing-proposals

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/finance/billing-proposals`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-112: GET /api/v1/finance/project-fundings

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/finance/project-fundings`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-113: GET /api/v1/crm/opportunities

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/crm/opportunities`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-114: GET /api/v1/core/sidebar-feed

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/core/sidebar-feed`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-115: GET /api/v1/projects/projects

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/projects/projects`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-116: GET /api/v1/finance/project-cost-entries

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/finance/project-cost-entries`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-117: GET /api/v1/crm/opportunities

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/crm/opportunities`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-118: GET /api/v1/inventory/stock-balances

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/inventory/stock-balances`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-119: GET /api/v1/master-data/products

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\feed.api.ts` invokes `GET /api/v1/master-data/products`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-120: GET /api/v1/finance/project-cost-entries

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\finance.api.ts` invokes `GET /api/v1/finance/project-cost-entries`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-121: GET /api/v1/finance/project-fundings

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\finance.api.ts` invokes `GET /api/v1/finance/project-fundings`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-122: GET /api/v1/finance/billing-proposals

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\finance.api.ts` invokes `GET /api/v1/finance/billing-proposals`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-123: GET /api/v1/projects/projects

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\finance.api.ts` invokes `GET /api/v1/projects/projects`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-124: GET /api/v1/finance/billing-documents

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\finance.api.ts` invokes `GET /api/v1/finance/billing-documents`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-125: GET /api/v1/finance/billing-proposals

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\finance.api.ts` invokes `GET /api/v1/finance/billing-proposals`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-126: GET /api/v1/finance/customer-receipts

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\finance.api.ts` invokes `GET /api/v1/finance/customer-receipts`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-127: GET /api/v1/finance/project-cost-entries

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\finance.api.ts` invokes `GET /api/v1/finance/project-cost-entries`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-128: GET /api/v1/projects/projects

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/projects`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-129: GET /api/v1/projects/main-tasks

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/main-tasks`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-130: GET /api/v1/projects/task-assignments

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/task-assignments`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-131: GET /api/v1/projects/weekly-tasks

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/weekly-tasks`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-132: GET /api/v1/projects/daily-tasks

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/daily-tasks`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-133: GET /api/v1/projects/tasks

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/tasks`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-134: GET /api/v1/projects/milestones

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/milestones`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-135: GET /api/v1/projects/readiness-checks

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/readiness-checks`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-136: GET /api/v1/finance/project-cost-entries

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/finance/project-cost-entries`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-137: GET /api/v1/finance/billing-proposals

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/finance/billing-proposals`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-138: GET /api/v1/finance/project-fundings

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/finance/project-fundings`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-139: GET /api/v1/accounts/users

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/accounts/users`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-140: POST /api/v1/projects/projects

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/projects`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-141: POST /api/v1/projects/customers

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/customers`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-142: PATCH /api/v1/projects/projects/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `PATCH /api/v1/projects/projects/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-143: DELETE /api/v1/projects/projects/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `DELETE /api/v1/projects/projects/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-144: POST /api/v1/projects/projects/:param/recalculate_health

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/projects/:param/recalculate_health`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-145: POST /api/v1/projects/projects/:param/advance_stage

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/projects/:param/advance_stage`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-146: POST /api/v1/projects/main-tasks

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/main-tasks`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-147: POST /api/v1/projects/tasks

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/tasks`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-148: DELETE /api/v1/projects/main-tasks/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `DELETE /api/v1/projects/main-tasks/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-149: DELETE /api/v1/projects/tasks/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `DELETE /api/v1/projects/tasks/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-150: POST /api/v1/projects/weekly-tasks

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/weekly-tasks`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-151: POST /api/v1/projects/tasks

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/tasks`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-152: DELETE /api/v1/projects/weekly-tasks/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `DELETE /api/v1/projects/weekly-tasks/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-153: DELETE /api/v1/projects/tasks/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `DELETE /api/v1/projects/tasks/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-154: POST /api/v1/projects/daily-tasks

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/daily-tasks`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-155: PATCH /api/v1/projects/daily-tasks/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `PATCH /api/v1/projects/daily-tasks/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-156: DELETE /api/v1/projects/daily-tasks/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `DELETE /api/v1/projects/daily-tasks/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-157: POST /api/v1/projects/task-transfers

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/task-transfers`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-158: GET /api/v1/projects/task-transfers

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/task-transfers`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-159: POST /api/v1/projects/task-transfers/:param/approve

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/task-transfers/:param/approve`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-160: POST /api/v1/projects/task-transfers/:param/reject

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/task-transfers/:param/reject`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-161: POST /api/v1/finance/project-cost-entries

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/finance/project-cost-entries`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-162: DELETE /api/v1/finance/project-cost-entries/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `DELETE /api/v1/finance/project-cost-entries/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-163: POST /api/v1/finance/project-fundings

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/finance/project-fundings`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-164: DELETE /api/v1/finance/project-fundings/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `DELETE /api/v1/finance/project-fundings/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-165: POST /api/v1/finance/billing-proposals

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/finance/billing-proposals`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-166: DELETE /api/v1/finance/billing-proposals/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `DELETE /api/v1/finance/billing-proposals/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-167: GET /api/v1/projects/projects/:param/financial-performance

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/projects/:param/financial-performance`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-168: POST /api/v1/projects/projects/:param/update_financials

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/projects/:param/update_financials`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-169: GET /api/v1/projects/projects/:param/funding_requests

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/projects/:param/funding_requests`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-170: POST /api/v1/projects/projects/:param/funding_requests

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/projects/:param/funding_requests`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-171: POST /api/v1/projects/milestones

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/milestones`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-172: POST /api/v1/projects/main-tasks/:param/assign_members

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/main-tasks/:param/assign_members`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-173: POST /api/v1/projects/task-assignments

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `POST /api/v1/projects/task-assignments`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-174: DELETE /api/v1/projects/task-assignments/:param

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `DELETE /api/v1/projects/task-assignments/:param`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-175: GET /api/v1/accounts/users

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/accounts/users`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.

## Q11-CONTRACT-176: GET /api/v1/projects/members

- **Given** the Express application is running and the caller has no bearer token.
- **When** frontend call site `frontend-next\lib\api\project.api.ts` invokes `GET /api/v1/projects/members`.
- **Then** the backend route exists and rejects the request with HTTP 401 before any business mutation.
